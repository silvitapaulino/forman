Drupal.locale = { 'pluralFormula': function($n) { return Number(($n!=1)); }, 'strings': { "The document already contains a teaser break. Do you want to proceed by removing it first?": "El documento ya contiene una marca de separación. ¿Quiere eliminarla?" } };